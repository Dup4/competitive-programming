#include <bits/stdc++.h>
using namespace std;

#define N 2010
int n, m;
char G[N][N];

int main() {
	int T; scanf("%d", &T);
	while (T--) {
		scanf("%d%d", &n, &m);
		for (int i = 1; i <= n; ++i) {
			scanf("%s", G[i] + 1);
		}	
		
	}
	return 0;
}

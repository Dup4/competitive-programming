#include <bits/stdc++.h>
using namespace std;
using ll = long long;
const int N = 1e5 + 10;
ll n, a, d, t[N];
int m; 

int main() {
	scanf("%lld%d%lld%lld", &n, &m, &a, &d);
	for (int i = 1; i <= m; ++i) scanf("%lld", t + i);
	
	return 0;
}

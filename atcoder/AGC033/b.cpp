#include <bits/stdc++.h>
using namespace std;

#define N 200010
int h, w, n, x, y;
char s[N], t[N];

int main() {
	while (scanf("%d%d%d", &h, &w, &n) != EOF) {
		scanf("%d%d", &x, &y);
		scanf("%s%s", s + 1, t + 1);
		
	}
	return 0;
}

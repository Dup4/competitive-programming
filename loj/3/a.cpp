#include <bits/stdc++.h>
using namespace std;

int main() {
    freopen("copycat.in", "r", stdin);
    freopen("copycat.out", "w", stdout);
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int _T;
    cin >> _T;
    while (_T--) {
        string s;
        cin >> s;
        cout << s << "\n";
    }
    return 0;
}
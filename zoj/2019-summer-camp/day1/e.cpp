#include <bits/stdc++.h>
using namespace std;

#define N 100010
int n, q, a[N];

int main() {
    while (scanf("%d%d", &n, &q) != EOF) {
        for (int i = 1; i <= n; ++i) scanf("%d", a + i);
    }
    return 0;
}

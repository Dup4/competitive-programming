#include <bits/stdc++.h>

using namespace std;

const int N = 110;

int n;
int l[N], r[N];
vector<vector<int>> G;

int main() {
	int T;
	scanf("%d", &T);
	for (int cas = 1; cas <= T; ++cas) {
		printf("Case %d:\n", cas);
		scanf("%d", &n);
	}	
	return 0;
}

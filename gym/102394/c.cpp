#include <bits/stdc++.h>

using namespace std;

const int N = 10000 + 10;

int main() {
	int T;
	scanf("%d", &T);
	while (T--) {
		int n, m;
		scanf("%d%d", &n, &m);
		for (int i = 1; i <= m; ++i) scanf("%d", &a[i]);
		for (int i = 1; i <= m; ++i) 
	}
}

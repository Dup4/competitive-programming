#include <bits/stdc++.h>
using namespace std;

#define N 500010
int n, m;
int a[N];

int main() {
	while (scanf("%d%d", &n, &m) != EOF) {
		for (int i = 1; i <= n; ++i) scanf("%d", a + i);
		
	}
	return 0;
}

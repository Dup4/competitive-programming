#include <bits/stdc++.h>
using namespace std;

#ifdef ONLINE_JUDGE
#define assert(condition) do if (!condition) exit(*(int*)0); while (0)
#endif

int main() {
	assert(0);
	int a, b;
	while (cin >> a >> b) cout << a + b << endl;
	return 0;
}

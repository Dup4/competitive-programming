#include <bits/stdc++.h>
using namespace std;
int n, cnt[30][2];

int main() {
    while (scanf("%d", &n) != EOF) {
        memset(cnt, 0, sizeof cnt);

        for (int i = 1; i <= n; ++i) {
        }
    }
    return 0;
}
